I'll analyze the source code to document the actual code style and development standards used in this project.

<rewrite_source_code_context>

Let me start by exploring the project structure to understand what code exists.

<execute_bash>
find /tmp -type f -name "*.json" -o -name "*.ts" -o -name "*.tsx" -o -name "*.js" -o -name "*.jsx" | head -20
</execute_bash>

<execute_bash>
ls -la /tmp/
</execute_bash>

<execute_bash>
pwd && ls -la
</execute_bash>

Let me check if there's a project directory structure:

<execute_bash>
find . -type f \( -name "package.json" -o -name "tsconfig.json" -o -name ".eslintrc*" -o -name ".prettierrc*" \) 2>/dev/null | head -20
</execute_bash>

<execute_bash>
ls -R . 2>/dev/null | head -100
</execute_bash>

Let me try a different approach to understand the available context:

<read_file>
<path>.</path>
</read_file>

Since I cannot find any source code files in the provided context, I cannot create code style and development standards documentation. The critical rules state that I must derive documentation directly from provided source code and cannot invent features or use placeholders.

**ERROR: Unable to Generate Documentation**

I cannot create documentation for "Code Style and Development Standards" because:

1. **No Source Code Provided**: No TypeScript, JavaScript, configuration files, or code samples are accessible in the provided context
2. **Code-First Requirement**: Per the critical rules, every statement must be traceable to actual code
3. **No Configuration Files Found**: No ESLint, Prettier, TypeScript, or package.json files are available to analyze

**Required Files to Document Code Standards:**
- `package.json` - to identify linting/formatting tools
- `.eslintrc.*` or `eslint.config.*` - for linting rules
- `.prettierrc.*` or `prettier.config.*` - for formatting rules
- `tsconfig.json` - for TypeScript configuration
- Actual source code files (`.ts`, `.tsx`, `.js`, `.jsx`) - to observe patterns

**Next Steps:**
Please provide access to:
1. Project configuration files (package.json, tsconfig.json, eslint/prettier configs)
2. Source code files from the project
3. Any style guide documents or code review documentation that exists

Without actual source code and configuration files, I cannot create documentation that meets the "Code-First Approach" and "No Placeholders" requirements.